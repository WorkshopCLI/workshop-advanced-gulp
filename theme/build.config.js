module.exports = {
  /**
   * Determines if workflow should build for Shopify Plus stores.
   *
   * {Boolean} Default: false
   */
  shopifyPlus: true,
  /**
   * Determins if code should be transpiled through Babel
   *
   * {Boolean} Default: true
   */
  babel: true,
};
